# 1. Bỏ qua mọi thứ trong thư mục gốc
/*

# 2. Ngoại trừ thư mục chứa source code (.java files)
!/src/

# 3. Ngoại trừ file .gitignore này
!.gitignore

# 4. Các ngoại lệ thường gặp khác (Dù đã bị bỏ qua bởi /*, đây là để rõ ràng hơn)
# Bỏ qua thư mục output của compiler (out)
/out/

# Bỏ qua các file cấu hình IDE (đã thấy trong Untracked files)
.idea/
*.iml

# Bỏ qua thư mục "git" (nếu đây là một thư mục)
/git/